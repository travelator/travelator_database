repos:
  - repo: https://github.com/psf/black
    rev: 24.2.0  # Use the latest stable version
    hooks:
      - id: black
  - repo: https://github.com/pycqa/flake8
    rev: main  # Use the latest stable version
    hooks:
      - id: flake8
        args: [--max-line-length=79]
